require 'spec_helper'

describe ReviewsController do
  describe "POST create" do
    context "with valid input" do
      it "creates the review" do
        
      end
      
      it "sets a user for the review"
      it "sets a video for the review"
      it "redirects to review's video page"
    end

    context "with invalid input" do
      it "doesn't create the review"
      it "renders the video show template"
    end
  end
end