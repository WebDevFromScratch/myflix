%article.video_category
  .videos.row
    - @videos.each do |video|
      .video.col-sm-2
        = link_to "" do #set path when I have a show view
          %img(src="#{video.cover_image_url}")
          