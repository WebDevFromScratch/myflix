%article.video_category
  .videos.row
    - @videos.each do |video|
      .video.col-sm-2
        = link_to video_path(video) do #set path when I have a show view
          %img(src="#{video.small_cover_url}")
          